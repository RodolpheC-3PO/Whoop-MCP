# Memo de configuration — NE PAS commiter de vrai .env.
# Ces variables sont normalement injectees via claude_desktop_config.json
# (champ "env"), pas via un fichier .env lu au runtime.
WHOOP_CLIENT_ID=ton_client_id_ici
WHOOP_CLIENT_SECRET=ton_client_secret_ici
WHOOP_REDIRECT_URI=http://localhost:3000/callback
